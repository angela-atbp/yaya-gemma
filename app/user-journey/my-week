Changes

1. src/components/AppLayout.tsx

Bottom nav (mobile): Remove /meal-planner entry from bottomTabs so the bar shows: Pantry · My Week · Home (FAB) · Recipes · Grocery.

Sidebar (desktop): Reorder navItems to:





/ Home



/pantry My Pantry



/ma-semaine My Week



/meal-planner Meal Plan



/recipes Recipes



/grocery-list Grocery List



/favorites For You



/pantry-health Kitchen Diary

No other layout/styling changes.

2. src/pages/MaSemaine.tsx

Add a Cook Now button to each plan day card whose actionType is cook or batch (next to / above the Swap button). On click, open the existing CookNowOverlay for that day's recipe.

Implementation details:





Import CookNowOverlay (and CookNowRecipe type) from @/components/CookNowOverlay.



Add state const [cookingDay, setCookingDay] = useState<DayPlan | null>(null);.



Build the CookNowRecipe from the DayPlan: { title: d.recipeTitle, ingredients_json: d.ingredients, instructions: d.instructions }.



Render the overlay full-screen when cookingDay is set (early return like AdaptedRecipePage), passing servings={cookingDay.portions ?? profile?.default_servings ?? 2}, lang, and onClose/onCooked that clear cookingDay.



Cook Now button styled as a primary action (bg-primary text-primary-foreground, min-h-[44px], ChefHat icon), grouped with the existing Swap button in a flex row.



Add i18n keys maSemaine.cookNow = "Cook now" / "Cuisiner maintenant" in src/lib/i18n/en.ts and fr.ts.

Out of scope

No backend / edge-function / DB changes. No changes to CookNowOverlay itself.
